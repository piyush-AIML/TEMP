# dependencies
node_modules/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# environment variables (never commit secrets)
.env*
!.env.example

# enquiry persistence (contains personal data — never commit)
data/enquiries.jsonl

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editors / tools
.vscode/
.idea/

# Claude Code — SHARED project config IS committed (.claude/rules, .claude/skills,
# .claude/hooks, .claude/settings.json). These two are per-machine, so they are not.
.claude/settings.local.json
.claude/active-project

# stage-run scratch (ledgers, briefs, reports, verification tables) -- was .superpowers/
.stage/

/src/generated/prisma
